title = "Terraform Custom Secret Detection"

[[rules]]
id = "generic-terraform-password"
description = "Hardcoded password found in Terraform configuration"
regex = '''(?i)(admin_password|password|secret|api_key)\s*=\s*"[^"]+"'''
keywords = [
    "admin_password",
    "password",
    "secret"
]